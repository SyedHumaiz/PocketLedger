import NetInfo from '@react-native-community/netinfo';
import { router, useFocusEffect } from 'expo-router';
import { useCallback, useState } from 'react';
import { Alert, FlatList, Pressable, SafeAreaView, StyleSheet, Text, View } from 'react-native';
import { deleteBudget } from '../../../src/api/budget-service';
import { useAuthStore } from '../../../src/auth/auth-store';
import { listLocalBudgets, removeLocalBudget, type LocalBudgetWithCategory } from '../../../src/db/budget-repository';
import { getDatabase } from '../../../src/db/database';
import { listActiveExpenses } from '../../../src/db/expense-repository';
import type { LocalExpenseRecord } from '../../../src/db/types';
import { budgetMutationError } from '../../../src/features/budgets/budget-errors';
import { budgetProgress, isOverBudget, remainingAmount, spendingForBudget } from '../../../src/features/budgets/budget-logic';
import { formatAmount } from '../../../src/features/expenses/expense-presenter';
import { AppButton, Card, OfflineNotice, StateMessage } from '../../../src/ui/components';
import { colors, screen, spacing } from '../../../src/ui/theme';
const today=new Date();
export default function BudgetsScreen():React.ReactElement {const user=useAuthStore(s=>s.user);const net=NetInfo.useNetInfo();const offline=net.isConnected===false||net.isInternetReachable===false;const [month,setMonth]=useState(today.getMonth()+1);const [year,setYear]=useState(today.getFullYear());const [budgets,setBudgets]=useState<LocalBudgetWithCategory[]>([]);const [expenses,setExpenses]=useState<LocalExpenseRecord[]>([]);const [state,setState]=useState<'loading'|'ready'|'error'>('loading');const [error,setError]=useState<string|null>(null);const load=useCallback(async()=>{if(!user)return;setState('loading');try{const db=await getDatabase();const [b,e]=await Promise.all([listLocalBudgets(db,user.id,{month,year}),listActiveExpenses(db,user.id)]);setBudgets(b);setExpenses(e);setState('ready');}catch{setState('error');}},[user,month,year]);useFocusEffect(useCallback(()=>{void load();},[load]));const previous=()=>{if(month===1){setMonth(12);setYear(v=>v-1);}else setMonth(v=>v-1);};const next=()=>{if(month===12){setMonth(1);setYear(v=>v+1);}else setMonth(v=>v+1);};const remove=(budget:LocalBudgetWithCategory)=>Alert.alert('Delete budget?','This removes the cached budget after the server confirms deletion.',[{text:'Cancel'},{text:'Delete',style:'destructive',onPress:()=>void (async()=>{if(!user)return;try{await deleteBudget(budget.id);await removeLocalBudget(await getDatabase(),user.id,budget.id);await load();}catch(cause){setError(budgetMutationError(cause));}})()}]);const label=new Date(year,month-1,1).toLocaleDateString('en-US',{month:'long',year:'numeric'});return <SafeAreaView style={screen}><FlatList contentContainerStyle={styles.content} data={state==='ready'?budgets:[]} keyExtractor={x=>x.id} ListHeaderComponent={<View style={styles.head}>{offline&&<OfflineNotice/>}<View style={styles.period}><Pressable onPress={previous}><Text style={styles.arrow}>‹</Text></Pressable><Text style={styles.periodText}>{label}</Text><Pressable onPress={next}><Text style={styles.arrow}>›</Text></Pressable></View><AppButton label="Add budget" disabled={offline} onPress={()=>router.push({pathname:'/budgets/new',params:{month:String(month),year:String(year)}})}/>{offline&&<Text style={styles.hint}>Connect to add, edit, or delete budgets.</Text>}{error&&<Text style={styles.error}>{error}</Text>}</View>} ListEmptyComponent={state==='loading'?<StateMessage kind="loading">Loading cached budgets…</StateMessage>:state==='error'?<StateMessage kind="error">Unable to load local budgets.</StateMessage>:offline?<StateMessage kind="offline">No cached budgets are available for this month.</StateMessage>:<StateMessage>No budgets for this month. Add an overall or category budget.</StateMessage>} ItemSeparatorComponent={()=> <View style={{height:spacing.sm}}/>} renderItem={({item})=>{const spent=spendingForBudget(item,expenses),over=isOverBudget(item.amountMinor,spent),progress=budgetProgress(item.amountMinor,spent),remaining=remainingAmount(item.amountMinor,spent);return <Card><View style={styles.row}><View><Text style={styles.name}>{item.categoryName??'Overall budget'}</Text><Text style={styles.muted}>{formatAmount(spent,'USD')} spent of {formatAmount(item.amountMinor,'USD')}</Text></View><View style={styles.links}><Pressable disabled={offline} onPress={()=>router.push({pathname:'/budgets/[id]',params:{id:item.id}})}><Text style={[styles.link,offline&&styles.disabled]}>Edit</Text></Pressable><Pressable disabled={offline} onPress={()=>remove(item)}><Text style={[styles.delete,offline&&styles.disabled]}>Delete</Text></Pressable></View></View><View style={styles.track}><View style={[styles.bar,{width:`${Math.min(progress,100)}%`},over&&styles.over]}/></View><Text style={[styles.remaining,over&&styles.overText]}>{over?`${formatAmount(Math.abs(remaining),'USD')} over budget`:`${formatAmount(remaining,'USD')} remaining`} · {progress}%</Text></Card>}}/></SafeAreaView>;}
const styles=StyleSheet.create({content:{paddingVertical:spacing.md},head:{gap:spacing.sm,marginBottom:spacing.md},period:{alignItems:'center',flexDirection:'row',justifyContent:'space-between'},periodText:{color:colors.text,fontSize:20,fontWeight:'800'},arrow:{color:colors.accent,fontSize:34,paddingHorizontal:spacing.md},hint:{color:colors.muted,fontSize:13},error:{color:colors.danger},row:{flexDirection:'row',justifyContent:'space-between'},name:{color:colors.text,fontWeight:'800',fontSize:16},muted:{color:colors.muted,fontSize:13,marginTop:3},links:{flexDirection:'row',gap:spacing.md},link:{color:colors.accent,fontWeight:'700'},delete:{color:colors.danger,fontWeight:'700'},disabled:{opacity:.4},track:{height:8,backgroundColor:colors.border,borderRadius:8,marginTop:spacing.md,overflow:'hidden'},bar:{height:'100%',backgroundColor:colors.accent},over:{backgroundColor:colors.danger},remaining:{color:colors.muted,marginTop:spacing.sm,fontSize:13},overText:{color:colors.danger}});
