import { router, useFocusEffect } from 'expo-router'; import { useCallback, useState } from 'react'; import { KeyboardAvoidingView, Platform, ScrollView, StyleSheet, Text, View } from 'react-native'; import { SafeAreaView } from 'react-native-safe-area-context';
import { useAuthStore } from '../../../src/auth/auth-store'; import { listLocalCategories } from '../../../src/db/category-repository'; import { getDatabase } from '../../../src/db/database'; import { insertExpense } from '../../../src/db/expense-repository'; import type { LocalCategoryRecord } from '../../../src/db/types'; import { amountToMinor, isValidExpenseDate } from '../../../src/features/expenses/expense-presenter'; import { usePreferencesStore } from '../../../src/preferences/preferences-store'; import { AppButton, Card, FormInput, StateMessage } from '../../../src/ui/components'; import { colors, screen, spacing } from '../../../src/ui/theme';
const today=()=>new Date().toISOString().slice(0,10);
export default function CreateExpenseScreen():React.ReactElement {const user=useAuthStore(s=>s.user);const preferences=usePreferencesStore(s=>s.preferences);const [amount,setAmount]=useState('');const [currency,setCurrency]=useState('');const [description,setDescription]=useState('');const [date,setDate]=useState(today());const [categories,setCategories]=useState<LocalCategoryRecord[]>([]);const [categoryId,setCategoryId]=useState<string>();const [error,setError]=useState<string|null>(null);const [saving,setSaving]=useState(false);const load=useCallback(async()=>{if(user){setCategories(await listLocalCategories(await getDatabase(),user.id));setCurrency(value=>value||preferences?.defaultCurrency||'PKR');}},[user,preferences?.defaultCurrency]);useFocusEffect(useCallback(()=>{void load();},[load]));const save=async()=>{if(saving)return;try{if(!user||!description.trim()||!categoryId||!isValidExpenseDate(date))throw new Error('Complete every field and use YYYY-MM-DD for the date.');const code=currency.trim().toUpperCase();if(!/^[A-Z]{3}$/.test(code))throw new Error('Currency must be a three-letter code.');setSaving(true);await insertExpense(await getDatabase(),{userId:user.id,categoryId,amountMinor:amountToMinor(amount),currency:code,description:description.trim(),expenseDate:date});router.back();}catch(cause){setError(cause instanceof Error?cause.message:'Unable to save expense.');}finally{setSaving(false);}};return <SafeAreaView style={screen}><KeyboardAvoidingView style={styles.flex} behavior={Platform.OS==='ios'?'padding':undefined}><ScrollView keyboardShouldPersistTaps="handled" contentContainerStyle={styles.content}><Text style={styles.title}>New expense</Text><FormInput label="Amount" placeholder="e.g. 20.50" keyboardType="decimal-pad" value={amount} onChangeText={setAmount}/><FormInput label="Currency" autoCapitalize="characters" value={currency} onChangeText={setCurrency}/><FormInput label="Description" value={description} onChangeText={setDescription}/><FormInput label="Date" placeholder="YYYY-MM-DD" value={date} onChangeText={setDate}/><Text style={styles.label}>Category</Text>{categories.length===0?<View><StateMessage>No categories are cached yet.</StateMessage><AppButton label="Open Categories" variant="secondary" onPress={()=>router.push('/categories')}/></View>:categories.map(c=><Card key={c.id}><AppButton label={categoryId===c.id?`Selected: ${c.name}`:c.name} variant={categoryId===c.id?'primary':'secondary'} onPress={()=>setCategoryId(c.id)}/></Card>)}{error&&<Text accessibilityRole="alert" style={styles.error}>{error}</Text>}<AppButton label={saving?'Saving…':'Save locally'} disabled={saving} onPress={()=>void save()}/></ScrollView></KeyboardAvoidingView></SafeAreaView>;}
const styles=StyleSheet.create({flex:{flex:1},content:{paddingVertical:spacing.md,gap:spacing.sm},title:{color:colors.text,fontSize:26,fontWeight:'800'},label:{color:colors.text,fontWeight:'800',marginTop:spacing.sm},error:{color:colors.danger}});
