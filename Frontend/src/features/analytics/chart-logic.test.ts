import test from 'node:test'; import assert from 'node:assert/strict'; import { lastSixMonthKeys,sixMonthSpending,spendingByCategory } from './chart-logic';
const e=(id:string,userId:string,date:string,amountMinor:number,currency='PKR',categoryId:string|null='c',deletedAt:string|null=null)=>({id,userId,categoryId,amountMinor,currency,description:'x',expenseDate:date,createdAt:date,updatedAt:date,version:1,deletedAt,syncStatus:'SYNCED' as const});
test('aggregates active user category spending deterministically',()=>{const values=spendingByCategory([e('1','u','2026-08-01',100),e('2','u','2026-08-02',200),e('3','other','2026-08-02',999),e('4','u','2026-08-02',999,'PKR','c','x')],[{id:'c',userId:'u',name:'Food',normalizedName:'food',createdAt:'',updatedAt:'',deletedAt:null,syncStatus:'SYNCED'}],'u','2026-08');assert.deepEqual(values,[{key:'PKR:c',label:'Food',currency:'PKR',amountMinor:300}]);});
test('generates zero-filled deterministic six month currency periods',()=>{assert.deepEqual(lastSixMonthKeys(new Date('2026-08-20T00:00:00Z')).map(x=>x.key),['2026-03','2026-04','2026-05','2026-06','2026-07','2026-08']);const values=sixMonthSpending([e('1','u','2026-08-01',250,'PKR'),e('2','u','2026-08-01',500,'USD')],'u',new Date('2026-08-20T00:00:00Z'));assert.equal(values.length,12);assert.equal(values.find(x=>x.key==='PKR:2026-03')?.amountMinor,0);assert.equal(values.find(x=>x.key==='USD:2026-08')?.amountMinor,500);});
