# Replace with local PostgreSQL credentials before database-backed features are added.
DATABASE_URL="postgresql://pocketledger:change-me@localhost:5432/pocketledger?schema=public"
PORT=3000
# Development-only placeholders. Generate a unique random secret before deployment.
JWT_SECRET="replace-with-a-random-secret-of-at-least-32-characters"
JWT_EXPIRES_IN="15m"
